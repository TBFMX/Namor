<%= form_for(@ad_group) do |f| %>
  <% if @ad_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_group.errors.count, "error") %> prohibited this ad_group from being saved:</h2>

      <ul>
      <% @ad_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :campaing_id %><br>
    <%= f.number_field :campaing_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :gr_id %><br>
    <%= f.number_field :gr_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
